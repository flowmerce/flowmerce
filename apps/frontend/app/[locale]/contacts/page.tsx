import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Contacts | Flowmerce',
};

const ContactsPage = () => {
    return <div className="p-80 mx-auto text-center">Contacts page</div>;
};

export default ContactsPage;
